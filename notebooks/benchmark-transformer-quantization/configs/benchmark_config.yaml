experiment_name: "Qwen2.5_Benchmark_CPUBaseline"

model:
  id: "Qwen/Qwen2.5-0.5B-Instruct"
  task: "text-generation"

benchmark:
  warmup_iterations: 3
  measure_iterations: 15
  batch_size: 1
  sequence_length: 128

export:
  cache_dir: "./ov_cache"
  precision: "int4"  # Options: int4, int8, fp32